# Plan — preserve valid proposals against stale notifications

**Goal:** Prevent an old review notification from invalidating an independently valid current correction proposal, and correct remaining misleading operator documentation.
**Created:** 2026-09-14 · **Status:** approved

## Tasks
- [ ] 1. Preserve a valid current proposal when processing an older receipt — backend/high
      Scope: loop/supervision_policy.go, loop/supervision_policy_test.go
      Accept: after evidence reconciliation at budget 2 of 2 an older receipt returns pending without changing a valid proposal ledger or immutable receipts, and replay of the current event remains proposed with the same child and budget → go test ./loop -run SupervisionCorrection; changed current job, altered artifact bytes, altered spec bytes and absence of a valid proposal still produce pending rather than preserving invalid authority → go test -race ./loop -run SupervisionCorrection; surrounding supervision regressions pass → go test ./loop -run Supervision
- [ ] 2. Correct remaining operator claims and record direct documentation evidence — backend/medium
      Depends on: 1
      Scope: README.md, README.pt-BR.md, docs/loop-supervision.md, docs/loop.md, cmd/batuta/main.go
      Accept: operator text distinguishes engine-run cancellation from interruption of post-exit verification and interrupted launch recovery, states the actual evidence directory versus engine artifacts, and never suggests a ticked completed plan is equivalent to original task status — direct read-only document inspection required; full suite passes with packages serialized → go test -p 1 ./... -timeout=15m; module builds → go build ./...

## Decisions and context

User already authorized validating and correcting remaining review findings. Base is e7f5a4b on feat/supervision-review-closeout. Fable completed 8/8 cohorts, Grok completed 18/24, several failures were timeout/format rather than defects. Current targeted tests pass. Independent go test -p 1 ./... -timeout=15m passed every package, loop 213.823s. Default go test ./... previously timed out after ten minutes during concurrent review workloads; root cause is not proven. Serialization is diagnostic validation, not a code fix or global profile change. Keep reviewed source stable while any review runs; those runs have now finished.

No change to attempt budget policy, child reservation identity, ownership policy, historical digests, global configuration or model routing. No merge to main, release, install, new dependency, remote notification or unrelated refactor. Other supervisor persistence/resume work remains separate. Go standard library, TDD, conventional commits, one verified task per commit. Known worker sandbox cache/preflight issues are environmental: report them and use writable temporary GOCACHE. Routine syntax fixes and expected red regressions are authorized; stop only for genuine unresolved scope/authority or two actual fixes failing for the same unexpected cause.

**Task 1.** Independently traced recipe: supervisionCorrectionReconciliationFixture(t, 2), successfully propose on the fresh event at Attempts=MaxAttempts=2, then replay first.EventID with first.Evidence.Digest. The unconditional !evidenceMatches persist at policy.go:363 overwrites current proposed with pending, preserving attempts. Next fresh event now returns exhausted. Return pending/review_evidence_mismatch to stale caller but leave an independently verified current proposed ledger untouched. Never return proposed to the stale input. Preserve only if previous evidence matches live canonical job digest, current job is reported REWORK/FIX_BEFORE_SHIP and actual artifacts/spec remain intact. Job digest alone does not detect tampered artifact bytes. Invalid existing proposal must still become pending. Add byte-preservation and replay assertions, both job change and bytes-only tampering regressions, and no-existing-proposal case. Preserve original child and attempts. Do not broaden to every historical notification or weaken receipt authentication.

**Task 2.** Help says all Review files are in artifacts but job.json, spec and source live in the parent job directory. Docs overgeneralize interruption after launch: cancellation while the engine runs maps to uncertain/cleanup_unresolved, post-exit verification can fail with execution_failed, and recovery of a interrupted durable launching state is uncertain with no cleanup outcome necessarily set. Status-only plan metadata equivalence does not make a ticked done archive equivalent: task checkbox state affects task-set digest. Explain precise semantics consistently. Documentation is in English; existing Portuguese translation may be kept consistent but do not introduce new Portuguese primary documents. Do not create brittle exact-word tests or grep gates mirroring prose merely to appease Grok: inspect changed claims directly and report evidence. Do not rewrite archived plan contracts, add timeout flags, or perform cosmetic cleanup.
