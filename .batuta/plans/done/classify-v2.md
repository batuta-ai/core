# Plan — plan classification v2: code-measured features, Jev yes/no questions, a fixed lane rule, outcome labels
<!-- inputs: profile.md@sha256:e18a00765937 routing.md@sha256:bdb31fda5c7d -->

**Goal:** Build the v2 classifier and its bench exactly as frozen in `.batuta/judge-research.md` section 13 (commit `ff2e68e`): code measures the task's Scope, Jev answers five `noul` questions, a fixed rule maps both to a lane, and `batuta judge classify bench --rubric v2` scores the lanes against recorded outcomes with the four frozen measures. v1 stays as it is; nothing routes on v2.
**Created:** 2026-09-26 · **Status:** done

## Tasks
- [x] 1. classify v2: Scope features, five noul questions, the frozen lane rule — backend/high
      Scope: classify/v2.go, classify/v2_test.go
      Accept: Features(task) returns the Scope file count, the count of distinct directories, TestOnly (every entry is a `*_test.*` file or under `testdata/`) and DocsOnly (every entry is `*.md` or under `docs/`), with glob entries counted as written → go test ./classify -run TestFeatures; BuildRequestV2 asks exactly the five questions `contract`, `lifecycle`, `security`, `open_decision` and `mechanical` as `judge.QuestionNoul`, with the wording of section 13, carries the same task text and bounded plan context as v1, and never carries the host lane → go test ./classify -run TestBuildRequestV2; DecideV2 applies the rule first-match: critical if open_decision; low if mechanical and at most 2 Scope files and none of contract, lifecycle, security; high if security, or at least two of contract, lifecycle, security, or at least 3 directories; otherwise medium → go test ./classify -run TestDecideV2Rule; an answer below 0.7 confidence or missing counts as yes for contract, lifecycle, security and open_decision and as no for mechanical, and the decision records which answers were defaulted → go test ./classify -run TestDecideV2Uncertainty; v1 behaviour is unchanged → go test ./classify -run 'TestBuildRequest$|TestDecide$'; the package stays green → go test ./classify
- [x] 2. batuta judge classify bench --rubric v2 scores against outcomes with the frozen measures — backend/high
      Depends on: 1
      Scope: cmd/batuta/judge_classify.go, cmd/batuta/judge_classify_test.go, docs/judge.md
      Accept: `batuta judge classify bench` accepts `--rubric v1|v2` (default v1, unchanged output); with v2 each task line adds the five answers with confidences and which were defaulted → go test ./cmd/batuta -run TestClassifyBenchV2Lines; the v2 summary reports: sufficient (candidate or retried) and insufficient (escalated or failed) counts, economy = share of sufficient tasks with judge lane ≤ plan lane, safety = share of insufficient tasks with judge lane > plan lane (marked `reported only` when fewer than 10 insufficient), the lane distribution with its largest share, balance = (economy + safety) / 2, a PASS/FAIL line per frozen measure, agreement with the plan lane, and unavailable answers; unknown outcomes are excluded from the measures and counted → go test ./cmd/batuta -run TestClassifyBenchV2Summary; `--json` emits the same measures → go test ./cmd/batuta -run TestClassifyBenchV2JSON; no request body carries the plan lane → go test ./cmd/batuta -run TestClassifyBenchV2LabelFree; docs/judge.md documents `--rubric v2` and points to section 13 of the research note → grep -q 'rubric v2' docs/judge.md; the package stays green → go test ./cmd/batuta ./classify

## Decisions and context

Go standard library only, conventional commits. Read `.batuta/judge-research.md` section 13 first; its wording, thresholds and rule are frozen and must be copied, not reinterpreted. Reuse v1's context bounding and secret filtering (`classify/classify.go` ~152–190) and the bench's outcome derivation (`benchOutcome`, `cmd/batuta/judge_classify.go` ~329) and journal reading (~278). `judge.QuestionNoul` is the yes/no question type (`judge/judge.go` ~39). Put v2 in new files so v1 and its tests stay untouched. Scope was checked with `grep -rln` for tests that call classify or the bench: only `classify/classify_test.go` and `cmd/batuta/judge_classify_test.go`, and the first must not change. Sandbox note: loopback listeners may be blocked for the test server in the sandbox; write the tests, and let the conductor's gate run them.
