# Plan — integrate validated supervision and verifier deliveries

**Goal:** Reconcile independently developed supervisor lifecycle/persistence fixes and verifier submission uncertainty with the reviewed automatic-review branch, preserving all contracts in one release candidate.
**Created:** 2026-09-14 · **Status:** done

## Tasks
- [x] 1. Integrate supervisor persistence and authorized resume with automatic review — backend/high
      Scope: cmd/batuta/main.go, cmd/batuta/main_test.go, docs/loop-supervision.md, docs/loop.md, README.md, README.pt-BR.md, executor/dispatch_test.go, loop/supervision.go, loop/supervision_io.go, loop/supervision_io_unix.go, loop/supervision_io_unix_test.go, loop/supervision_io_windows.go, loop/supervision_io_windows_test.go, loop/supervision_notify_test.go, loop/supervision_policy.go, loop/supervision_policy_test.go, loop/supervision_test.go, loop/supervision_review.go, loop/supervision_review_test.go
      Accept: integrated observer reconciles exact deleted legacy refs, bounded platform-correct persistence rejects nonregular inputs, and authorized question answers resume once while uncertain ownership stays blocked → go test -race ./loop -run Supervision; an authorized resumed delivery can complete and enter automatic review while correction proposals never implicitly resume and stale evidence cannot invalidate a current valid proposal → go test ./cmd/batuta ./loop -run Supervision; timeout regressions require no installed Codex executable → go test ./executor -run TestDispatchTaskTimeoutExits124AcrossTransports -count=1; full suite passes → go test -p 1 ./... -timeout=15m; module builds → go build ./...
- [x] 2. Integrate independent verifier submission uncertainty — backend/high
      Depends on: 1
      Scope: loop/attempt.go, loop/attempt_test.go, loop/runner.go, loop/settle.go, loop/settle_test.go
      Accept: verifier submission uncertainty remains distinct and persists across resume without replaying possibly submitted external work, preserving integration recovery behavior → go test ./loop -run 'Verifier|Uncertain|Settlement|Settle'; integration does not bypass pending verifier evidence or supervisor cleanup gates → go test -race ./loop -run 'Verifier|Uncertain|Supervision'; full suite passes → go test -p 1 ./... -timeout=15m; module builds → go build ./...

## Decisions and context

User explicitly approved proceeding with integration after latest patch validation. Target is isolated feat/release-integration based on f9aa03b from feat/supervision-review-closeout. Never merge to main, release, publish, install globally, or change routing/providers in this task. Main checkout has unrelated managed state; do not reset or overwrite it. No new dependencies. One verified integration task per commit. Local reference branches and commits exist and can be inspected with git; do not require source worktree mutation.

The target contains automatic deep review plus three correction deliveries. Latest stale-notification patch: Grok SHIP with complete coverage, Fable only optional redundant guards/style suggestions, independent go test -json -p 1 ./... -timeout=15m passes every package (loop 280.949s). Prior review full-suite results diverged under simultaneous workloads; root cause is unresolved, not proven fixed. Use serialized package validation without changing project-wide profile. Preserve explicit state checks rather than refactoring just to eliminate optional findings.

Work test-first on integration-specific behavior. Existing reviewed source tests must survive integration. Go standard library, platform build compatibility, frozen exported signatures, conventional commits. Keep current attempt-budget semantics and reserved child identities. No blanket approval, replay of uncertain execution, inferred cleanup success or fabricated usage savings. Documentation defaults to English, existing translations may remain consistent. Worker sandbox cache/preflight limitations are environmental; use temporary writable GOCACHE and report restrictions. Expected regression failures and routine syntax corrections are authorized. Stop for actual unresolved Scope/authority, or two fixes failing for the same unexpected cause, not merely two invocations including expected red tests.

**Task 1.** Source branch feat/supervision-fixes at e8f16df, common supervisor base 252ff12. Desired source implementation commits: 75cec37 (exact terminal ref reconciliation), 64477bd (authorized bound answer resumes runner), 42f9a24 (bounded platform persistence), 3455a82 (provider-independent timeout fixture). Plan/bookkeeping commits on source do not belong to this task and must not replace target WORK/plan history. Reconcile behavior rather than blindly preferring one side of conflicts. Target review policy has full-contract binding, immutable receipts, stale-event safety and child-reservation preservation absent from source: retain them. Target Supervise always configures review via CLI; source resume must coexist without duplicate runner or reviewer launches. Task-specific behavioral test must cover resumed delivery completion followed by review, not just separate old suites. Do not bypass symlink/ownership ambiguity. Native Windows runtime qualification is unavailable on this macOS host and must remain explicit. If imported source tests conflict with new contracts, explain and prove the intended combined behavior instead of dropping assertions. Update relevant operator docs accurately; no generic cleanup.

**Task 2.** Desired source implementation is cf83b75 from feat/acp-dispatch (branch head 48f1018), missing from target ancestry; preserve only its verifier uncertainty behavior and tests. Its predecessor 231cd3f is already ancestral to target. Review diff cf83b75^..cf83b75, reconcile with task1 result, and verify matching retry/recovery semantics. Do not enable stock ACP production transport or claim cleanup qualification: factory wiring and process cleanup qualification remain separate known release questions. Scope is intentionally those five changed files; report evidence if actual integration needs more.
