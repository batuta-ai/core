# Plan — close remaining supervision review findings

**Goal:** Resolve confirmed remaining reservation and regression-proof defects, then align operator documentation without expanding supervision policy.
**Created:** 2026-09-13 · **Status:** approved

## Tasks
- [ ] 1. Recover an existing correction reservation after evidence reconciliation — backend/high
      Scope: loop/supervision_policy.go, loop/supervision_policy_test.go
      Accept: a real proposed correction followed by stale evidence and valid reconciliation can recover its own reserved child identity without self-conflict → go test ./loop -run SupervisionCorrection; other reviews cannot claim that child and a changed policy cannot replace an existing child identity or reset budgets → go test -race ./loop -run SupervisionCorrection; full contract and receipt regressions remain passing → go test ./loop -run Supervision
- [ ] 2. Make cancellation and historical receipt regressions discriminating — backend/high
      Depends on: 1
      Scope: loop/supervision_process_unix_test.go, loop/supervision_review_test.go, loop/supervision_policy_test.go, publication/command_review_unix_test.go
      Accept: cleanup ownership proof actually observes lock contention instead of merely expiring before acquisition and FIFO release remains reliable after engine termination → go test ./loop ./publication -run 'Supervision|ReviewProcess' -count=3; replay does not inherit the one millisecond cancellation deadline and historical tampering reaches the historical receipt digest guard → go test -race ./loop -run Supervision; test helpers clean up all owned processes without weakening uncertain-state assertions → go test ./publication -run ReviewProcess -count=3
- [ ] 3. Describe actual evidence and cancellation semantics — backend/medium
      Depends on: 2
      Scope: README.md, README.pt-BR.md, docs/loop-supervision.md, docs/loop.md, cmd/batuta/main.go
      Accept: docs distinguish pre-launch execution failure, ownership-acquisition error, and post-launch unresolved cleanup, and accurately state the CLI-fixed review timeout → go test ./cmd/batuta -run Supervision; docs explain operator byte digest plus equivalent parsed task/title/goal/context contract rather than claiming exact byte equality with reviewed spec, and use a slug placeholder in the evidence path → go test ./loop -run SupervisionCorrection; full suite passes → go test ./...; module builds → go build ./...

## Decisions and context

User explicitly approved validation and correction of the latest Fable 5.1 and Grok 4.6 findings. Base 894152d is the completed six-task review-fixes delivery. Independent Fable coverage was 5/6 and Grok 4/6; rejected formatting/timeouts leave review incomplete, not approved. Correct confirmed defects before spending another full review on unchanged code. Root's adjudication is recorded in .batuta/runs/review-closeout-judgment.md. Original supervisor persistence/resume fixes remain separate and are not to be silently merged or reimplemented here.

Preserve existing attempt accounting, ownership policy, historical journals/digests, immutable evidence and frozen exported APIs. No main merge, release, installation, global adapter edit, routing/model change, new dependency or remote notification. Code is Go standard library, TDD, conventional commits, isolated task worktrees. One verified task per commit. Expected red regressions and routine syntax fixes are already authorized. A routine failure does not itself require user permission. Stop only on unresolved scope/authority or after two actual fix attempts fail for the same unexpected cause, then report evidence. Worker sandbox Go-cache/preflight scratch failures are environment limitations; use writable temporary GOCACHE and report those limits, conductor gates run outside sandbox.

**Task 1.** In supervision_policy.go persist() overwrites the job row with pending while preserving previous.Correction.Delivery on evidence mismatch. After evidence reconciliation the uniqueness scan sees that same row as an existing reservation. Reproduce this through the public policy path before changing production code. A narrow own-row exclusion may be part of the fix, but must not permit changing the previously reserved child via policy replacement, release its reservation to another review, bypass fresh immutable evidence, reopen an executed child or reset exhausted budgets. Preserve idempotent return of an existing proposal. Add explicit cross-review conflict and child-policy-change regressions. No policy redesign: charging actual local validation attempts remains as documented. If the original finding is false under a required invariant, document the disproof instead of forcing a change.

**Task 2.** Fable findings: timeout replay reuses a 1 ms deadline, ownership waiter may expire before trying the OS lock, late FIFO writer may encounter ENXIO after the reader is killed, and tampered current receipt hits the earlier current-event guard before the new historical digest guard. Use explicit fixture synchronization and direct ownership evidence, not simply increasing sleeps. A durable launching field alone does not establish that the OS lock is still held. Keep FIFO handles and helper cleanup robust on failure without hiding premature engine exit. For historical tampering, move current job evidence so the old receipt loader is actually reached and assert its specific rejection. Use a normal replay deadline independent of the intentionally tiny timeout test. No blanket test parallelization or production test flags. This task owns tests only; genuine newly discovered production defects require an evidence-backed scope decision.

**Task 3.** Current default one hour has no supervise CLI flag. Timeout during ownership acquisition may return before a job transition, probe/snapshot interruption uses failed/execution_failed, and launched engine interruption uses uncertain/cleanup_unresolved. Describe these accurately without promising automatic replay or verified descendant cleanup on unsupported systems. Policy PlanEvidence.Digest authenticates operator supplied bytes, while the reviewed spec is separately byte-authenticated and compared by task digest/title/goal/effective context. Status-only metadata equivalence is deliberately tested: fix docs rather than forcing new byte-equality policy. Style-only findings, broad deduplication, lock polling optimization and cosmetic error normalization are deferred; no fresh features in this closeout.
