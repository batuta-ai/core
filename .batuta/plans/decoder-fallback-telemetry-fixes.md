# Plan — decoder-fallback-telemetry review fixes
<!-- inputs: profile.md@sha256:e18a00765937 routing.md@sha256:bdb31fda5c7d -->

**Goal:** Close the findings of the review of branch `fix/decoder-fallback-telemetry` by claude/opus (verdict FIX_BEFORE_SHIP, run with `--reviewer claude/opus` because codex authentication is failing): the dropped-line counter counts normal events, the new tail files are outside the authorized review artifacts, and review path redaction lacks the loop's preceding-character guard.
**Created:** 2026-09-25 · **Status:** approved

## Tasks
- [ ] 1. Only unknown event types count as dropped; tail files are authorized, pruned and redacted like the loop — backend/medium
      Scope: executor/decode.go, executor/decode_test.go, executor/tail.go, executor/tail_test.go, review/report.go, review/report_test.go, review/lint_test.go, review/session.go, review/session_test.go, review/findings.go, loop/attempt.go, loop/judgment.go, loop/verifier_policy_test.go, gates/gates.go, cmd/batuta/main.go
      Accept: a decoder increments the dropped count only in the branch for an event type it does not know; known events without text (codex thread.started, turn.started, item.started, item.completed of type command_execution or reasoning; claude system, user and rate_limit_event allowed; cursor system and tool_call; agy step_update without text_delta; opencode step_finish without tokens) do not count → go test ./executor -run TestDecoderCountsOnlyUnknownEvents; ArtifactPaths (review/report.go ~273) returns every cohort tail file the report produces, so the review publication runner's tree guard excludes them → go test ./review -run TestArtifactPathsIncludeCohortTails; WriteArtifacts removes cohort-*.tail.txt files in the directory that the current report does not produce, and only files matching exactly that pattern → go test ./review -run TestWriteArtifactsPrunesStaleTails; path redaction and secret-line dropping move into one shared helper in executor (for example executor/tail.go beside Tail) used by both loop and review, keeping the loop's guard that skips a match preceded by '.', '\\' or an alphanumeric byte, so `a/b.go` survives and `/private/x` becomes `x` → go test ./executor -run TestRedactPathsKeepsRelative && go test ./loop ./review; gates exports the no-TASK-lines signal as a constant and loop/attempt.go compares against it → grep -q 'SignalNoTaskLines' gates/gates.go && grep -q 'gates.SignalNoTaskLines' loop/attempt.go; review/findings.go declares the `<<<FINDINGS` marker once and ParseFindings and hasFindingsOpeningMarker both use it → grep -q 'findingsOpen' review/findings.go && grep -q 'findingsOpen' review/session.go; table subtests in TestUncoveredCohortWritesTail and TestVerifierFailureKeepsOutputTail call t.Parallel() → go test ./review ./loop -run 'TestUncoveredCohortWritesTail|TestVerifierFailureKeepsOutputTail'; the packages stay green → go test ./executor ./review ./loop ./gates

## Decisions and context

Go standard library only, conventional commits. Behaviour otherwise unchanged. `redactText` and `dropSecretLines` are in `loop/judgment.go` (~515, ~666); move them rather than duplicate, and keep loop's callers working. `cmd/batuta/main.go` (~1190) builds the review publication runner from ArtifactPaths; touch it only if the signature must change. Sandbox note: `/bin/ps`, loopback listeners and scratch directories outside the worktree are blocked; run the named tests and let the conductor's gate run `go test ./...`.
