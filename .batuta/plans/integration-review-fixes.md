# Plan — fix integration review recovery defects

**Goal:** Correct the remaining task-dispatch and bound-answer recovery defects in draft PR #86 and add discriminating integration regressions.
**Created:** 2026-09-15 · **Status:** approved

## Tasks
- [ ] 1. Honor recorded task dispatch reconciliation requirements — backend/high
      Scope: loop/attempt.go, loop/attempt_test.go, loop/settle.go, loop/settle_test.go
      Accept: task ACP shutdown uncertainty blocks execution even when no prompt was submitted and remains blocked after resume → go test ./loop -run 'Dispatch|Uncertain|Reconciliation|Verifier'; no task or verifier possibly submitted work is retried implicitly → go test -race ./loop -run 'Dispatch|Uncertain|Verifier'; full suite passes → go test -p 1 ./... -timeout=15m
- [ ] 2. Recover durable answers after ownership stop errors and prove CLI continuation — backend/high
      Depends on: 1
      Scope: loop/supervision_policy.go, loop/supervision_policy_test.go, loop/supervision_test.go, cmd/batuta/main_test.go
      Accept: a matching durable bound answer is recovered even when the prior ledger reason is bound_answer_rejected after append, without duplicate answers or budget resets → go test -race ./loop -run 'SupervisionPolicy|SupervisionContinuation'; waiting_input with pending ref deletion remains pending with zero attempts and unchanged journal → go test ./loop -run SupervisionPolicy; CLI test traverses a real waiting question into answer and runner continuation with explicit execution settings and stdout/stderr separation → go test ./cmd/batuta -run Supervision; full suite passes → go test -p 1 ./... -timeout=15m; module builds → go build ./...

## Decisions and context

User repeatedly authorized review validation and corrections, now explicitly checking whether requested adjustments were done. This plan implements the two remaining confirmed Fable findings and related Grok behavioral proof gaps, not a new feature. Base 7edd058, feat/release-integration, draft PR https://github.com/batuta-ai/core/pull/86. Both integration reviews have finished: Fable covered 7/7 with FIX_BEFORE_SHIP, Grok 3/7 with timeouts. CI passed. Preserve original reviewed snapshots and report uncertainty honestly. Do not mutate main, merge the PR, release, globally install, change routing, add dependencies, or rewrite historical digests. Only conductor pushes verified updates to PR branch after this delivery.

Go standard library, test-first, conventional commits, isolated worktrees and one verified task per commit. Preserve original authorization, actual ownership checks, budgets and immutable evidence. Code/test changes must be within task Scope. Expected red regressions and routine syntax fixes are authorized. Known sandbox Go cache/preflight restrictions are environmental: use writable temporary GOCACHE, report restrictions, and let conductor gates verify outside sandbox. Stop only for genuine scope/authority ambiguity or two actual fixes failing for the same unexpected cause. Do not turn every expected test failure into a permission question.

**Task 1.** dispatchExecution journals ReconciliationRequired for transport failure shutdown even without submission. runAttempt currently checks requiresReconciliation(result) or execution error plus mayHaveSubmitted, ignoring the broader recorded flag. Failure/recovery paths in attempt.go and settle.go currently honor verifier flags but not all task flags. Trace all relevant consumption points and demonstrate the missing no-submission shutdown case before fixing. Preserve immutable dispatch evidence and explicit blockerSubmissionUncertain state. Do not replace every check mechanically if its semantics differ. Add same-boundary recovery regression without real provider calls; keep regular CLI failure retry behavior unchanged.

**Task 2.** answerDelivery appends KindAnswer before ownership.stop. On stop failure, InterveneSupervision persists bound_answer_rejected although the answer is already durable. Current recovery only recognizes answer_attempt_recorded. Recover from validated bound answer and matching policy identity rather than a single reason label; do not authorize a changed policy, mismatched question/answer, or unsafe ownership. Add post-append stop-failure equivalent and mismatch regressions, prove no duplicate append or budget reset. Grok's pending_ref_deletions branch gap is valid: add a waiting_input terminal deletion-intent scenario proving no mutation or attempt. CLI TestLoopSupervisionRoutedExecutionSettings currently uses an already-completed journal and cannot prove settings reach a resumed worker. Use an owned fake executor and actual waiting question through run(), assert durable answer/new execution, relevant resolved settings and output separation. No production test flags, blanket test refactors or optional style changes. If actual command wiring needs production changes beyond Scope, report the exact evidence for conductor scope adjustment.
