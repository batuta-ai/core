# Plan — close supervision cancellation and discovery boundaries

**Goal:** Resolve the remaining automatic-review findings with discriminating cancellation, relative-path and clean-home tests before whole-branch review.
**Created:** 2026-09-17 · **Status:** approved

## Tasks
- [ ] 1. Fix supervision cancellation and skills discovery boundaries — backend/high
      Scope: cmd/batuta/main.go, cmd/batuta/main_test.go, loop/supervision_run.go, loop/supervision_run_test.go, loop/supervision_review.go, loop/supervision_review_test.go, loop/profile.go, loop/profile_test.go
      Accept: cancellation during final review after implementation completed exits 130 after owned activity joins, while a joined independent observer/runtime failure remains visible and durable completed implementation is not rewritten as undone → go test ./cmd/batuta -run 'Loop|Supervis'; relative --skills from a caller outside the workspace resolves to the selected absolute directory for review and does not silently select a different installation → go test ./cmd/batuta -run 'Loop|Supervis'; passive standalone supervision with isolated home, no BATUTA_SKILLS and no installed skills still delivers local events for a journal without a review candidate, while required review cannot silently succeed when adapters are unavailable → go test ./cmd/batuta -run 'Loop|Supervis'; supervision cleanup and writer/policy regressions pass → go test -race ./loop -run 'SupervisionRun|SupervisionReview|SupervisionPolicy'; full suite passes → go test -p 1 ./... -timeout=15m; build passes → go build ./...

## Decisions and context

The user authorized continuing necessary corrections. Base aa8898f contains completed original implementation plus a first correction pass. The automatic review again returned REWORK: two blocker findings and a violated cancellation criterion. Conductor inspected the owning code and accepts all three. This is a focused correction, not feature expansion. Preserve all successful tests including arbitrary-writer serialization, the persistent review gate, integrity, identity, ownership and uncertainty guards. No releases, globals, dependencies, broad refactors or altered historical review records.

Evidence: .batuta/reviews/supervision/2c669d4be4e5fd7a734a18e544332cb9014c632e5583d1b0d2b875fee7680103/artifacts/review.md. At cmd/batuta/main.go:734 standalone supervision calls FindSkills eagerly although passive observation does not require adapters. At :823 resolved skills can remain relative because FindSkills only EvalSymlinks; review runs inside a snapshot. At loop/supervision_run.go:103 cancellation during final review returns prior StateDone with context.Canceled; loopRunExit recognizes pure cancellation only with StateCanceled. Existing passing tests missed these exact conditions.

Write discriminating regressions first: real subprocess cancellation specifically while the final reviewer is active after implementation finalization, relative skills path with caller cwd different from workspace and snapshot, and isolated-home passive local-sink observation without review candidate. Do not mask path bugs with global BATUTA_SKILLS or installed skills. Do not mask unrelated joined failures using errors.Is alone. Changing the foreground exit classification must not discard completed implementation or claim review accepted; uncertain interrupted review must retain its recovery protections. Resolve explicit selections consistently at normal and supported standalone CLI paths. Missing implicit/default skills may be deferred until review needs them; a required review failure remains blocked, never silent success. Avoid process-global environment/cwd changes in parallel tests; use subprocess fixtures and scoped injected environment.

Before reporting done, check each review scenario against the actual changed code and independently distinguish command exit semantics from durable delivery state. Tests that merely inspect a helper or set the missing environment do not establish the reported end-to-end criterion. Full-branch review from released base 1257ece follows this task; these local fixes do not establish whole-release readiness.

Expected TDD red failures do not count as repeated unexplained failures. Runtime adapter preserves workspace sandbox and allows only existing integration scratch; use writable temporary GOCACHE when needed. Stop with exact evidence if further scope is required. No weakening or skipping tests to obtain green.
