# Triage — Tempo MVP retrospective (2026-09-21)

Source: the user's retrospective of batuta running the `tempo` weather MVP on the `hermes` machine through a plugin of theirs (verbatim copy, in Portuguese, in `2026-09-21-tempo-mvp-retro.pt-BR.md` beside this file). Evidence lives on that machine under `/home/francisross/Projects/tempo/.batuta/` (plan `plans/done/tempo-weather-mvp.md`, deliveries `tempo-weather-mvp-20260920-131750/135110/140350`, review `7328d43d…`). This triage maps every finding to the code as it is on core `main` today and to a proposed follow-up. Checked in the code on 2026-09-21; each "cause" line names the file.

| # | finding | cause in the code today | severity | follow-up |
|---|---|---|---|---|
| 4.1 | claude executor "cannot run Bash or Git"; tasks parked in `waiting_input`/`blocked`, conductor ran the gates by hand | `skills/batuta/adapters/claude.md` `run:` uses `--permission-mode acceptEdits`, which accepts file edits but still prompts for every Bash command; headless `claude -p` gets no answer, so tests, build, lint and git are denied. agy and codex adapters run with full permissions inside the worktree. No preflight checks that an executor can run commands before the brief is sent. | P0 | (a) skills: `claude.md` run lines use `--dangerously-skip-permissions` (the worktree is the sandbox; document the trade like agy.md does). (b) core: a capability preflight per dispatch: run the adapter with a one-line brief (`print BATUTA-CAPABLE after running: git status --porcelain && echo ok`) before the first real prompt; a route that cannot complete it fails as `executor_incapable` before any work exists and escalates one row up without a submission-uncertain state. Record executor, model, transport and the probe result in the journal. |
| 4.2 | ownership between executor, runner and conductor unclear when the executor cannot run gates | The graph already has one owner per state (`routing/graph.go`: pending → preparing → running → candidate/waiting_input/blocked → integrated) and `loop/report.go:866` (`answerSelected`) refuses to answer while a runner is live. What is missing is a conductor-side operation that takes a parked attempt, runs the gates in the worktree and either integrates or records a failure, with provenance. Today the conductor does this by hand (`cherry-pick`, `git worktree remove`). | P0 | core: `batuta loop --recover <delivery> <task>`: locks the delivery, checks out the parked ref in the existing worktree, runs the four gates through the same code as `runAttempt`, records `gates_reported` with `source: conductor`, and either records a candidate or a failure with feedback. Idempotent: refuses when the parked ref is already integrated. Docs: the state table in `docs/loop.md` gains the owner column the retrospective asks for. |
| 4.3 | manual `cherry-pick` and worktree removal to recover parked commits | Same as 4.2; `--resume` only continues a delivery whose runner died, it does not reconcile a parked ref against a branch that moved (`loop/settle.go:91` refuses when HEAD moved and points to `--abandon`). | P1 | Covered by `--recover` above plus a `--reconcile` dry run that prints the diff between the parked ref and the branch, conflicts included, without touching the workspace. |
| 4.4 | vitest and eslint scanned `.batuta/**` (worktrees, snapshots, review artifacts); gates failed on orchestrator files | `worktree.Dir = ".batuta/worktrees"` (`core/worktree/git.go:26`) puts every worktree inside the repository. Go tooling skips dot-directories, JavaScript tooling does not. `batuta-init` only adds `.git/info/exclude` entries (`skills/batuta-init/SKILL.md` step 11); nothing patches `vitest.config.*`, `eslint.config.*`, `tsconfig.json`, `jest.config.*`, `pytest.ini`. | P1 | (a) skills: `batuta-init` and `batuta doctor` detect known runners/linters and add `.batuta/**` to their ignore lists, or warn when they cannot. (b) core: an option to place worktrees outside the repository (`os.UserCacheDir()/batuta/worktrees/<repo-digest>/`), default for JavaScript/TypeScript stacks once the skills know it; journals already store absolute `worktree_root`, so nothing else moves. (c) cleanup on done/abandon: remove orphaned worktrees and, best effort per OS, processes whose working directory is inside a removed worktree (three `next dev` servers from `chico.ai` worktrees were found alive six days later on 2026-09-20 on this machine). |
| 4.5 | task 4 returned to `waiting_input` more than once after a limited grant | An answer is one text (`loop/report.go` `Answer`); the executor's next session reads it in the brief but nothing persists a grant of files/operations bound to `task_id`+`execution`, so a fresh session asks again when it reaches the same boundary. | P1 | core + skills: the answer may carry a structured grant (`allow: paths, operations, until`) that the brief renders as a standing authorization for that task and execution; a new question must state what differs from the standing grant, else the runner answers it from the grant without parking. |
| 4.6 | review reported `incomplete_coverage`, all criteria `satisfied`, `Findings: None`, `Verdict: REWORK`, `findings.json` = `null` | `review/report.go:30` builds `var findings []Finding` (nil) and writes it as JSON, so an empty list serializes as `null`. `BuildReport` (`review/report.go:23-24`) forces `Rework` whenever a cohort is uncovered, by design ("incomplete coverage never ships"), but the text gives no structured reason; the supervision side does distinguish (`loop/supervision_review.go:473` `incomplete_coverage`), the CLI output does not. | P0 | core: `findings.json` always a list; a `review_failures` list in the report and in `findings.json` (kind `operational`, with the cohort, the exit code and the tail) whenever coverage is incomplete; `review.md` prints `Verdict: REWORK (review incomplete: cohort 1 exit 1)` and exit code 4 for "review incomplete" so `REWORK` (3) is reserved for product defects. `classifySupervisionReview` and the exit-code contract are updated together. |
| 4.7 | `Coverage: 0/1 cohorts`, `Lint: not configured` while `pnpm lint` ran as evidence | Coverage counts reviewer cohorts over the diff, not plan criteria; `Lint: not configured` comes from the missing `Lint:` line in `.batuta/profile.md` (`review/lint.go:27`, `review/report.go:102`), independent of what the executor ran. | P1 | docs + skills: `batuta-init` writes `Lint:` when it finds a lint script; the report prints the five dimensions separately (diff coverage, criteria coverage, gates executed, lint configured, reviewer failures) as the retrospective lists them. |
| 4.8 | three blockers (stale geolocation callback, loading state after denial, no visual highlight on the active combobox option) missed by the review | The review reproduces criteria and reads the diff; it does not propose adversarial scenarios. | P1 | skills: an adversarial review pass (`batuta-review --adversarial` or a section of the review prompt) with the retrospective's scenario list (out-of-order async callbacks, retry after success, permission changes mid-session, concurrent actions on one state source, keyboard/focus/`aria-selected` plus visual state, unmount with pending request, missing or partial API). Findings from it must carry a deterministic reproduction. The judge can help here later: `noul` "this criterion's proof exercises a concurrent or asynchronous path" to decide when the adversarial pass is mandatory. |
| 4.9 | app worked on `localhost`, the Tailscale origin served HTML without React hydration; `HTTP 200` did not catch it | Outside the loop; no acceptance criterion type for "final origin". | P2 | skills: an `Accept` idiom for the delivered origin (URL, server mode, one interactive action verified, no blocked resources in the server log), documented in `batuta-plan` and `batuta-qa-run`. |
| 6 | metrics: tasks accepted without intervention, `waiting_input` count, attempts, executor switches, executor time vs recovery time, manual commands, recovered commits, orphan worktrees, findings by source, cost and latency per route | Most are already in the journal (attempts, questions, answers, limit fallbacks, verifier and judge records with latency); none is summarized. | P2 | core: `batuta trail --metrics <delivery>` printing the retrospective's list, including `autonomy = tasks integrated without an answer or a conductor recovery / tasks`. |

Order proposed for the next plans, after the judge work in flight: (1) 4.1a in skills (one line, immediate), (2) 4.6 review contract in core, (3) 4.1b capability preflight in core, (4) 4.2/4.3 `--recover`, (5) 4.4 isolation and cleanup, (6) 4.5 grants, (7) 4.7/4.8/4.9 in skills, (8) metrics.
